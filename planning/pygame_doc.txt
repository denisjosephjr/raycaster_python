This Document will be used to learn about how PyGame works. (It is based off of 
SDL)

pygame.init() -- initializes all PyGame submodules so they are ready for use.
    *There are 6 submodules: pygame.display, pygame.event, pygame.font, 
    pygame.joystick, pygame.key, and pygame.mixer
