# game settings
RES = WIDTH, HEIGHT = 1600, 900  # So, I suppose we start with a variable called RES which is short for resolution
FPS = 60 # RES appears to be assigned to a tuple that unpacks the width and height into two integer values.
# feels strange to use capital letter variable names consider i always used snake case. Actually, they are special names.
# The icons change if I mod them.
PLAYER_POS = 1.5, 5 # mini map
PLAYER_ANGLE = 0 
PLAYER_SPEED = 0.004
PLAYER_ROT_SPEED = 0.002